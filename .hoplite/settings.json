{
  "version": 1,
  "ports": {
    "preview": 3000,
    "additional": {}
  },
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "npm install && npx prisma generate && (command -v git-lfs >/dev/null || { apt-get update -qq && apt-get install -y -qq git-lfs; })"
    },
    "run": {
      "enabled": true,
      "command": null
    },
    "archive": {
      "enabled": true,
      "command": null
    },
    "check": {
      "enabled": true,
      "command": null
    }
  },
  "mcpServers": []
}
